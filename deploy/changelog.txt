0.5.4.221  Cave maps redesigned: start fully solid, carve horizontal corridors+chambers+shafts from rock (Worms Armageddon style).
0.5.4.220  Update screen smaller font; terrain sky clear on hills/canyon maps.
0.5.4.219  Garcia damage 45/hit; A release required before weapon charge.
0.5.4.197  Fix opponent name showing as your own in live matches; Propeller Hat blade scaled correctly.
0.5.4.196  Black Hole renders as swirling void; Plasma Torch fires in mid-air; CPU uses full weapon set; soldiers squirm in fire.
0.5.4.182  Propeller Hat blades now spin, with direction and speed following the current wind.
0.5.4.181  Cowboy Hat, Pirate Hat, Viking Helm, Beanie, Bandana, Angel Ring, Flamethrower, Rocket Launcher, and SMG cosmetics now all draw correctly on your soldier in-game (previously shop-icon only).
0.5.4.180  New cosmetics: Beanie, Bandana, and Angel Ring hats, plus an SMG gun style. Boot cosmetics (all colors) now use a redesigned sprite shape in-game.
0.5.4.179  Server-side: match logs now record fall damage and drowning deaths with proper labels, and disconnect/reconnect events include the player's name (no client-visible change).
0.5.4.177  Server-side: each live match now also logs to its own match-N.log file alongside the main server log (no client-visible change).
0.5.4.176  Server-side: game server now logs to a text file for easier debugging (no client-visible change).
0.5.4.175  Quitting a live ranked match now warns you that it counts as a loss with an ELO penalty before confirming.
0.5.4.174  Title screen now offers RECONNECT/ABANDON if you lose connection mid-match (not on a normal exit), letting you rejoin the same paused live match.
0.5.4.173  Live ranked matches now pause (up to 3 min) if your opponent's connection drops, letting them reconnect and resume the same match instead of forfeiting.
0.5.4.171  Redesigned hat/gun cosmetics with full pixel-art remakes, sprite-based boots and equip-screen icons, taller Wizard Hat, and 6 new cosmetics (Blue Party Hat, Cowboy Hat, Pirate Hat, Viking Helm, Revolver, Flamethrower, Rocket Launcher).
0.5.4.168  Update-available screen now shows only the 5 most recent changelog entries, drawn at 2x font size (was 24 lines at 1x).
0.5.4.167  Perf: fix background sky-band double-paint — the row-memcpy bg pass now only covers rows that are sky for every visible column (the screen's minimum sky_limit); columns with taller sky get their extra band filled once during the terrain copy instead of being painted twice, shrinking the "terrain+bg" pixel-write total on terrain with height variance (cliffs/canyons/floating islands).
0.5.4.166  Perf: rewrite the background sky-band copy (parallax layer) to use per-row slice copies instead of per-pixel reads/writes — this was the single largest pixel-write section ("terrain+bg") in the profiling overlay, and is now a handful of contiguous memcpys per frame instead of up to ~150k individual pixel ops.
0.5.4.165  Fix soldiers spawning in sealed/unescapable cave pockets on caverns maps: cave spawn placement now flood-fills (walk/fall/jump) from the candidate floor to verify it actually connects to an open-to-sky exit, not just that one exists somewhere nearby with walls in between.
0.5.4.164  Fix black borders on the 6 BG1.png-derived backgrounds (bg_0..3, bg_extra_city, bg_extra_pyramids): these slices had a ~3-9px near-black border baked in from the source contact sheet's grid lines, which scaled up to a visible black edge along the top/sides of the screen. The border is now cropped before scaling, so the art stretches to fill the screen.
0.5.4.163  Perf: merge background and terrain viewport copies for cave/chasm/overhang columns into a single pass — background now only paints the sky band above the terrain (sky_limit), and the terrain copy fills the gaps between solid spans with background pixels directly, so each pixel in the column is written once instead of twice. Preserves background parallax.
0.5.4.162  Add per-section pixel-write profiling overlay (TEST mode): a new on-screen breakdown shows how many pixels each render section (terrain/bg, water, soldiers, fire, projectiles, HUD, etc.) writes per frame, to help identify the next 30fps optimization target.
0.5.4.161  Restore background parallax without re-introducing pixelation: the background cache now stores the image at native 1:1 resolution (one cache column per source-image column), and the per-frame viewport copy re-samples it with a parallax-shifted (0.10) column offset, wrapping within the cached image width. Crater-driven cache updates removed (cache is no longer terrain-dependent).
0.5.4.160  Fix background pixelation: the 0.5.4.158 parallax fix stretched each source image column across ~10 world pixels (0.10 parallax factor applied directly to the world-x -> image-x mapping), making the art blocky. Background cache now maps world columns 1:1 to image columns (tiled); this layer scrolls 1:1 with the world like terrain, with no parallax.
0.5.4.159  Bazooka direct hits capped at 50 damage; bee stings reduced to 5 damage each; bazooka smoke trail now spawns from the rocket's tail instead of its nose; background images now bilinear-scaled (was nearest-neighbor, looked pixelated on upscale); perf: cave/chasm terrain columns now use precomputed solid spans in the viewport copy instead of a per-pixel is_solid check (~230k checks/frame removed).
0.5.4.158  Background cache now preserves the slow camera parallax (baked into the world-x -> image-x mapping, so it's still a one-time precompute) and the rotation pool includes all 6 BG1.png slices (15 total backgrounds, up from 13).
0.5.4.157  Major perf fix: the BG2 sky background was being re-rendered per-pixel from the source image every frame, painting down to the waterline (~640x400px) for every column with a cave/chasm/floating-island air gap. Now pre-rendered once per map into a world-space cache and stamped into the viewport with a handful of row memcpys per frame. Also: added background variants sliced from BG1.png to the rotation pool (was BG2.png's 9 only).
0.5.4.156  Perf: the every-frame framebuffer blit (180-degree screen rotation on Miyoo Mini Plus) reversed each row one byte at a time with per-byte indexing (640px x 4 bytes x 480 rows = ~1.2M indexed ops/frame); now copies whole 4-byte pixels via chunked slice copies. This runs unconditionally every frame regardless of scene contents, so it's the most likely remaining 25->30fps gap.
0.5.4.155  Perf: fire-patch flames now use unchecked pixel access for the common in-bounds case (was bounds-checking every pixel of every flame row, up to ~250 checks per burning fire/soldier each frame), closing more of the 25->30fps gap.
0.5.4.154  Fix soldiers unable to move on any uphill slope: the horizontal-move sweep checked each step against the soldier's current foot height with no step-up allowance, so even a 1px rise in terrain ahead halted movement entirely (projectiles, which don't use this check, were unaffected). The sweep now allows the same 0-8px step-up as the destination check.
0.5.4.153  Fix light-blue patches appearing on fresh maps (not just craters): spawn-mound headroom carving could punch a gap into solid ground without invalidating the sky_limit/solid_to_water cache, causing the viewport copy to block-copy stale cached pixels there. Perf: soldier skeleton line drawing now bounds-checks once per line instead of per pixel.
0.5.4.152  Fix soldiers still getting stuck on invisible terrain: is_on_ground/jump_unstick_lift now check the full 3-column body footprint (was center-only), matching the movement collision check. Fix camera shaking left-right during multi-explosion turns (was flip-flopping between explosions/airborne soldiers; now follows whichever is closest to current camera position). Perf: water surface now uses unchecked pixel access.
0.5.4.151  Fix stale light-blue patches over explosion craters (crater carve now recomputes sky_limit/solid_to_water so the viewport copy doesn't block-copy the old cached pixels over the hole); perf: viewport copy and sun-glow now use unchecked pixel access in their hot per-pixel loops, closing the rest of the 25->30fps gap.
0.5.4.150  Fix soldiers getting stuck on invisible terrain edges (surface-snap now checks the full 3-column body width, matching movement collision); fix 25fps regression from v149's ghosting fix (background air-gap paint now writes pixels directly instead of per-pixel bounds-checked set_pixel); dp.sh now also publishes the nginx build/zip and version.txt on every deploy.
0.5.4.149  Fix ghosting on maps with chasms/caves/craters: background now repaints air gaps below the surface (the v148 sky-band clip left them stale, showing the title screen / persistent wind particles & explosions / blacked-out terrain). Sky-band clip kept only for fully-solid columns.
0.5.4.148  New backgrounds: 9 painted skies (from BG2.png), one picked per map by seed for variety across all archetypes; backgrounds now only drawn in the sky band (perf); faster fill_rect/fill_circle (clamp-once + contiguous row writes) for crates/barrels/mines/fire/black holes
0.5.4.147  Tighten soldier/terrain collision (full body width+height checks while walking/airborne, fixes clipping through walls/ceilings); spawns now require the same full-body clearance so soldiers never start wedged in terrain; perf: block-copy fully-solid terrain columns in viewport copy
0.5.4.146  Fix: last-resort spawn mounds raised on previously-empty columns were never rendered (stale spawn_y/sky_limit) - soldier appeared to float above invisible terrain
0.5.4.145  Perf: water-surface fill no longer rewrites the ~110 already-correct body rows per column (was 640x122 set_pixel/frame, now ~640x10)
0.5.4.144  Perf: skip per-pixel sky checks above the terrain surface in viewport copy (iterate only from sky_limit down, instead of scanning the full sky band)
0.5.4.143  Floating-island maps (archetype 2) had ~57% empty columns - increase land density (lower density threshold) so terrain coverage is more reasonable
0.5.4.142  Fix invisible terrain on seeds with cliff overhangs/floating islands above y=80 (unclamped sky_limit field for renderer)
0.5.4.141  Restrict water-surface redraw to the visible viewport (was rendering all 1920 world columns every frame, 3x oversized)
0.5.4.140  Major perf fix: clip static bg image draw to on-screen columns (was rendering ~2M px/frame across hidden tiles)
0.5.4.139  FPS counter moved above timer; removed cloud/ridge landform layers (perf+visual)
0.5.4.138  Add FPS counter (bottom-right); optimize bg sky-fill/cloud/copy loops via spawn_y bound (skip per-pixel is_solid)
0.5.4.137  Remove landform layer (was hiding terrain + heavy perf cost); other bg perf/visibility fixes
0.5.4.136  Fix bg layering/perf: terrain visible again, debris spread across screen, faster static bg
0.5.4.133  Worms-style backgrounds: biome skies, sun, parallax hills, wind debris
0.5.4.132  Hand of Jerry sprite now fully white including the bolt
0.5.4.120  live parity: death blasts, graves, blood, crate types
0.5.4.94  update notes now served live
0.5.4.92  backflip animates every time
0.5.4.91  fix garbled terrain on chasm maps
0.5.4.90  full map seed shown in test mode
0.5.4.89  fix garbled textures (new tiles)
0.5.4.88  plasma torch, crate + texture fixes
0.5.4.82  plasma torch weapon
0.5.4.81  spread spawn mounds, no flat slab
0.5.4.80  charge meter from gun muzzle
0.5.4.78  even spawn spread, instant win on wipe
0.5.4.77  wider spawn spacing, seed shown in test
0.5.4.76  bigger maps (3 screens), smaller black hole
0.5.4.75  chasms central, fair team home landforms
0.5.4.74  team spawns cluster on real landforms
0.5.4.73  varied terrain: ledges, pits, water chasms
0.5.4.72  stronger hills, more headroom, labels up
0.5.4.71  rolling terrain, headroom, cave spawns
0.5.4.67  removed pillar maps
0.5.4.66  real caves, overhangs, sky islands
0.5.4.65  bigger maps, more hazards, MP sfx
0.5.4.64  no jump-clip through ground
v0.5.4.135: static background images (sunset/aurora/jungle/canyon by biome) as new lowest render layer
