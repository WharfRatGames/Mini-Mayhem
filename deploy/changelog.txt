0.5.4.383  Miyoo framebuffer blit optimisation: batch all row reversal into a heap buffer, flush to mmap in one copy instead of 480 separate writes.
0.5.4.382  Weapon menu cells wider (80→92px) so names don't overlap.
0.5.4.381  Weapon menu 5 columns (was 4); test mode now includes Pistol, Clump Bomb, Molotov Cocktail.
0.5.4.380  Fix auto-update reliability: boot check timeout 5s→8s; live/TAT always checks (ignores sentinel); shows "CHECKING FOR UPDATES..." before live/TAT entry; fresh-check timeout 5s→6s.
0.5.4.379  Fix auto-update on Miyoo: fall back to local Pi IP (10.0.0.123) when router hairpin NAT blocks public hostname.
0.5.4.378  Clump Bomb: bigger fragments (blast 35px, 45dmg), red tapered-top icon, renamed to "CLUMP BOMB"; fix pistol sound (_play_once skips if busy — no retry delay); .110 sound investigation.
0.5.4.377  Cluster Bomb added (2x loadout, common crate) — 5 fragments in W/A flower pattern, 1s fuse; Banana Bomb reworked to 6 radial fragments that explode on contact; pistol shot sound (pistol.wav).
0.5.4.376  Fix slow startup / lobby timeout: version check now HTTP-only (3s max, no TLS handshake, no double-wait on failure); outer wait 5s.
0.5.4.375  Fix update check missed on slow wifi: HTTP timeout 2s→6s, outer wait 3s→15s; same fix for pre-MP check.
0.5.4.374  Pistol labelled correctly in HUD; camera locked during pistol burst; OnionOS menu button fixed (exec binary so keymon tracks correct PID); faster Miyoo framebuffer blit (row reversal via cache-local buffer).
0.5.4.373  Fix casual lobby live input drop (jump/backflip): same pressed-event merge fix applied to casual_conn inline reader, which the 372 fix missed.
0.5.4.372  Fix live input drop: server merges pressed events across frames instead of overwriting; Pistol labelled correctly in weapon menu; soldier can't move while weapon menu is open.
0.5.4.371  Pistol added: infinite, 6-shot burst ~0.75s apart, 5 dmg/shot; Molotov 2 per loadout, 12 flames on impact; MAC-10 infinite ammo, removed from crate pool.
0.5.4.370  All-modes parity enforced: aim_angle now passed through server_tick so Up/Down reach cursor-phase weapons (homing missile, airstrike, any future weapon) without special-casing in server code; parity test suite covers all 5 execution paths.
0.5.4.369  Fire physics: WA-style terrain sliding — flames roll downhill and collect in pits; soldiers squirm + get pushed by fire; squirm now synced to live clients.
0.5.4.368  IRC channel dashboard at /ircdash/ — live user lists, chat feed, ZPG and DiceRPG leaderboards; reads BenBot logs, no bot connection needed.
0.5.4.367  Molotov Cocktail added; MAC-10 starts with 2 uses; Shotgun and Ninja Rope infinite ammo; more varied terrain; update check before splash.
0.5.4.366  Fix live play unresponsiveness: reader thread held stream mutex during blocking reads, stalling InputMsg sends up to 33ms; cave crate drops now land inside cave boundaries.
0.5.4.365  Cave map FPS fix: row-major renderer replaces per-pixel stride copies; 30fps on all map types.
0.5.4.364  Fix 3s freeze when entering multiplayer after an OTA update; fix API SQLite locking under load.
0.5.4.363  Landmine blast radius 43→55px, trigger radius 25→35px; no knockback from jumps/backflips; horn hat position fix.
0.5.4.362  Windows auto-update on launch and before multiplayer; fix update check being silently skipped.
0.5.4.361  Account screen: Up/Down to select LOG IN / NEW ACCOUNT, A to confirm.
0.5.4.360  Show ping (ms) below opponent avatar in live mode; green/yellow/red by quality.
0.5.4.359  Airborne soldiers knock back targets they land on.
0.5.4.358  Hat centering fixes: devil horns (hat_9) raise 4px + shift 1px left; ninja band (hat_11) raise 5px (brim detection was finding tail instead of headband).
0.5.4.357  Soldier arms never occlude head — draw head/hat last in V2 render order.
0.5.4.356  Fix TLS cert timestamp (was July 2025, cert renewed June 2026 — broke bug reports and OTA).
0.5.4.355  Bug report: show actual error message on failure; increase thread stack to 2MB.
0.5.4.354  Fix hat gaps for all 43 hats (scale-corrected dy offsets); fix bug reports not sending (thread was detached before HTTP request completed).
0.5.4.353  Fix Onion OS menu button becoming select after exiting game.
0.5.4.352  Fix remaining client freeze when opponent quits live match; fix soldiers moving while weapon menu open; dragon skull gap fix; OTA update streams to disk (low-RAM fix).
0.5.4.351  New accounts must confirm password on registration.
0.5.4.350  Admin reset-password button on dashboard; login prompts player to choose new password when reset; hat size fixes (26 hats rescaled; dragon skull gap fixed).
0.5.4.349  Worm Hat cosmetic (warbond); hat 22 + 36 position/scale fixes; player search in dashboard; API error handling; lowercase usernames on register.
0.5.4.348  Match results post to Discord (add opponent name to API); homing missile cursor now scrolls screen in live mode; fix live_opp_username not populated for casual matches.
0.5.4.347  Fix TLS for Miyoos with wrong system clock (1971 etc) — use fixed timestamp for cert validity check instead of system time.
0.5.4.346  Fix TLS cert validation for Miyoo clients (embed ISRG Root YR); HTTP fallback for OTA updates when TLS fails; forced update screen when cert is broken (no skip).
0.5.4.345  arty_api.py: wrap handle() in top-level try/except so unhandled exceptions return 500 instead of closing the socket raw (was causing nginx "upstream prematurely closed connection"); add traceback logging to main accept loop so future crashes appear in journalctl.
0.5.4.345  Pirate tricorn bigger (scale 0.55) and repositioned to sit on head covering top half; war helm shop icon scaled to 1/3 size so it doesn't dominate the cell.
0.5.4.344  Worm Hat (hat_43) added as warbond item (80wb); wired into sprite array and store catalog; hat_43.png copied to deploy assets.
0.5.4.343  Pirate tricorn smaller (covers top half of head only); cavern ceiling sealed (dilation clamped to rock band, final re-seal after connectivity); cave map soldiers all spawn underground (WA style); removed white sub-menu header labels; VS screen text gold; war helm scales to head-size (replaces head, proportional).
0.5.4.343  Pirate tricorn smaller (covers top half of head only); cavern ceiling sealed (dilation clamped to rock band, final re-seal after connectivity); cave map soldiers all spawn underground (WA style).
0.5.4.342  Bug report screen: dimmed screenshot background, Keyboard widget for description, multi-select categories with checkboxes; bug reports routed through Mayhem Bot to #bug-reports; bot dashboard for posting/editing Discord messages; patch notes include GitHub release link.
0.5.4.341  War helm replaces head; pirate tricorn smaller with skull & crossbones; name/HP lift per-hat height; torch tunnels wide enough to walk back through; cavern map ceiling sealed; ceiling shafts wide enough for soldiers; bug report multi-select categories.
0.5.4.340  Hat sizing fixes: top hat smaller/higher, war helm and demon king horns scaled down, pirate tricorn fully redrawn with detailed tricorn silhouette; dashboard grant-all-cosmetics button with multi-select; Jellyfin traffic filtered from log viewer.
0.5.4.339  MENU button opens in-game bug reporter: category picker, on-screen keyboard, PNG screenshot attached and sent to Discord; fix soldier name/HP box overlap with hats (hat_lift 21->37).
0.5.4.338  Warbond store tab: two-tab shop (Scrap / Warbonds), WB balance in header, premium hat catalog with WB prices, warbond package placeholders; hat catalog now includes hats 37-42.
0.5.4.337  5 premium warbond hats (38-42): Cosmic Crown, Phoenix Crest, Void Wraith Hood, Gilded Jester, Crimson War Mask; Void Wraith and War Mask replace head; Ben's Birthday Hat (37, exclusive).
0.5.4.336  Hats scaled up proportionally to match larger V2 soldier head (1.75x: 70x63 base, updated anchor and per-hat offsets); server now requires exact version match.
0.5.4.335  Update screen: show patch notes at launch instead of auto-downloading; only online modes force the update; server accepts any client >= MIN_VERSION so server rebuild only needed on wire protocol changes.
0.5.4.334  Reduce fire patch DoT: 1 HP every 10 ticks (3 HP/s) down from 1 HP every 5 ticks (6 HP/s).
0.5.4.333  Backflip animation: root-rotation approach — skeleton spins as a unit, legs tuck up and arms hug in during the flip instead of counter-rotating.
0.5.4.332  Squad preview screen: foot position and name label adjusted for larger V2 soldier head.
0.5.4.331  Soldier facial features: ear nub, eyebrow, eye white+pupil+glint, nose dot, chin shadow, downturned mouth; suppressed for face-replacing hats (Viking, Dragon Skull, Luchador).
0.5.4.330  Cartoony soldier redesign (bigger head, knee joints, shoulder ball joint, belt+buckle, chest highlight, boots, wider arm, bigger gun); hitscan hitboxes match new proportions; cavern maps sealed top with varied terrain instead of flat block; opponent's move screen styled to match other screens, 5s→2s.
0.5.4.329  Cosmetics equip screen: Y=preview shows full squad with cosmetics; store/equip open instantly from cache; soldier-to-soldier blocking uses full body box; Dragon Skull 20% bigger; wildcard TLS renewal hook.
0.5.4.328  FPS: replace per-pixel modulo with conditional wrap in sky-band fill; throttle crater cache to 2/frame to kill dip spikes.
0.5.4.327  FPS optimizations: cut ambient debris particles, halve FX cap; fix weapon cycling in live mode, Dragon Skull scale, TNT/barrel instant-trigger.
0.5.4.326  Fix live multiplayer frame rate: background reader now yields after each state message so main thread can send input without blocking.
0.5.4.325  Debug logging for casual lobby investigation.
0.5.4.324  Fix casual lobby: second player now receives lobby state correctly.
0.5.4.323  Fix gamepad button mapping on Windows for PlayStation controllers.
0.5.4.322  Log full TLS login error to /tmp/arty_login_err.txt for debugging.
0.5.4.321  Fix login TLS: embed ISRG Root YR trust anchor missing from webpki-roots.
0.5.4.320  Fix login TLS failure caused by new Let's Encrypt root cert (ISRG Root YR).
0.5.4.319  Login error now shows actual failure reason instead of generic network error.
0.5.4.318  40 new terrain textures (textures5 atlas).
0.5.4.317  Windows desktop build; account register/logout on title screen; lobby fix for cross-platform play.
0.5.4.316  DNS pre-resolved at launch so live connect is instant instead of 15-20s.
0.5.4.315  Fix lobby freeze (B=cancel now works while waiting for opponent); Pharaoh headdress redesign (lappets flank head); Dragon Skull replaces head, flips to face soldier direction.
0.5.4.314  36 new cosmetic hats + 22 gun skins wired up; 1500-2000 scrap premium tier (Obsidian Crown, Pharaoh Headdress, Demon Horns, Astronaut Helmet, Dragon Skull, Obsidian Cannon, Crystal Sniper, Dragon's Breath, Blood Revolver, Thunder Rail); all skins unlocked for existing accounts.
0.5.4.313  Mine redesign (flat disc); flame flicker fix; faster match start; match history on profile screen; buy cosmetics from equip screen; 9 new backgrounds.
0.5.4.312  Multiplayer now encrypted with TLS; server reads cert from ~/mayhem-server/tls/.
0.5.4.311  End screen text uses full screen height; headline larger; mine flat-topped dome shape; torch flames restored; homing missile smoke trail; L1+aim for precision aiming.
0.5.4.310  Ranked matchmaking built into game server; homing missile locks soldier in place after firing; body colors match bazooka with red nose.
0.5.4.309  Homing missile charge fix; death blast explosion; server message logging.
0.5.4.308  Homing missile: target-locked message, death explosion knockback fix, red-tip visual; Mac-10 sound stretch; auto-update on startup.
0.5.4.304  Homing missile weapon added; comprehensive parity tests.
0.5.4.303  Sine LUT for debris and wind; Mac-10 burst fix; flame flicker fix.
0.5.4.302  Sine LUT for water waves; mine radius increased; X glyph fix.
0.5.4.301  New Mac-10 sound; 19-shot burst; mine damage 35.
0.5.4.300  Head-clipping fix; wider plasma torch tunnels; longer Mac-10 sound.
0.5.4.297  Cavern tunnel width and spawn reachability fixes; soldier collision fix.
0.5.4.296  Mac-10 looping fire sound.
0.5.4.293  Mac-10 weapon added (SMG with sprite); viking helm gap fix; waterline raised.
0.5.4.292  Torch tunnel fix; HP animation slower; fire gravity; grenade settle fix.
0.5.4.291  HP countdown animation; soldier stuck fix; terrain height; font X fix.
0.5.4.290  Airstrike sound on impact; plane spawn fix; viking helm full replacement.
0.5.4.289  Grenade settle fix; death FX tuning; rope and death bugfixes; viking helm.
0.5.4.288  Weapon menu 3-column sort; apex-based fall damage; Uzi sound fallback.
0.5.4.287  Uzi weapon — hitscan SMG burst, 20 shots over 2 seconds.
0.5.4.286  Minigun Worms-style recoil and dirt FX on terrain hits.
0.5.4.285  Minigun hits barrels and mines; test mode shows map archetype.
0.5.4.284  Minigun sound and 20-shot burst over 3 seconds.
0.5.4.283  Minigun weapon — crate-only rare, 20-shot auto-burst.
0.5.4.282  Idle gun held lower; sacred ordnance launches soldiers higher; viking helm replaces head; luchador mask smaller; sacred ord 8-frame direction-aware rotation.
0.5.4.281  Update is now mandatory; cannot be skipped for any mode.
0.5.4.278  Terrain generates lower on screen; water level lowered; more sky headroom.
0.5.4.277  Cave maps: soldiers now spawn on top crust or underground (alternating per slot); charge bar white outline for visibility on any background; SFX pop fix.
0.5.4.276  Charge bar white outline + shadow for visibility on any background; SFX pop fix (silence tail + drain padding); button controls moved to Settings; weapon controls kept in How To Play.
0.5.4.275  Meteor bomb chain explosions (barrel/mine) now scatter fragments upward; backflip more lenient on slopes and edges; bazooka no longer detonates immediately when fired upward near terrain; settings screen adds controls reference.
0.5.4.274  Air Strike throws soldiers sideways at an angle (not straight up); flame damage no longer launches victims airborne; menu cohesion pass (shared palette, button hints, selection highlight); Settings screen added; Profile screen added; TAT match end now shows full game-over overlay with ELO and scrap earned; post-match scrap earned shown in all modes.
0.5.4.273  Scrap earned shown on game-over screen; TAT end screen personalized per player; menu navigation polish (consistent wrap, Back=B); airstrike blast radius and force increased.
0.5.4.272  Fix false turn-end trigger after mine/fire/black-hole with no damage dealt.
0.5.4.271  Barrel surface placement fix; wall-stick fix; accurate FPS counter via inter-blit interval.
0.5.4.270  Accurate FPS counter via inter-blit interval measurement.
0.5.4.269  Doubled ambient particle counts across all terrain archetypes.
0.5.4.236  Live: weapon ammo now synced from server — selected weapon and ammo counts always accurate; camera follows airborne soldiers (knockback) nearest to center.
0.5.4.235  Live camera: tracks airstrike cursor during targeting phase; follows nearest explosion when projectiles land.
0.5.4.234  Live mode parity fixes: wind particles now drift across full screen; explosion FX animate; soldiers interleaved across map (not side-locked); airstrike plane/bombs now visible to opponent.
0.5.4.233  Equip screen: press A to enter cycling mode for selected cosmetic, dpad left/right to cycle, A to confirm, B to cancel. START saves all changes.
0.5.4.232  Casual live mode now uses a 4-player lobby: up to 4 players join, pick a team colour (red/blue/green/yellow) and ready up; match starts when all (2+) are ready. Lobby shows each player's name, avatar and colour. In-game names and health bars use each team's chosen colour.
0.5.4.231  Wind particles fixed: no longer disappear when camera scrolls; fill full airspace to waterline. Airstrike direction label centered at top of screen.
0.5.4.230  Plasma torch: longer flame with visible beam; no longer propels soldier through air; damages enemy soldiers in beam path.
0.5.4.229  Wind particles now fill full airspace down to waterline; particle counts increased across all biomes.
0.5.4.228  Air Strike: more damage (75, radius 38); cursor moves on Y axis; no sound until bombs drop.
0.5.4.227  Air Strike: bombs now drop as plane passes over target; yellow crosshair cursor; direction label top-center; tighter bomb spread.
0.5.4.226  Air Strike weapon: crate-only, unlocks after 7 turn cycles; cursor targeting with 5 drop markers; plane flies in from left dropping 5 bombs in sequence.
0.5.4.225  Bat launches soldier before damage; hat icons bigger in equip screen; wind particles fill full airspace to waterline with more density; Halo redrawn as profile-view ring; barrel placement fixed (no longer clips into terrain); wind meter redesigned (magnitude bar, no empty gap); black terrain seam fix.
0.5.4.224  Worms-style spawning: both teams scatter across full map (no fixed sides); cave soldiers spawn underground; more wind particles throughout full screen height; Pirate Tricorn hat now readable in-game; equip screen hat icons correct proportions; charge meter thinner at start.
0.5.4.223  Cave maps now have two corridor levels (upper+lower) with connecting shafts and dead-end tunnels; all soldiers spawn underground on cave maps; hats 19-24 now show in equip screen; Fez no longer floats; MP menu condensed (CASUAL launches directly, no extra PLAY screen).
0.5.4.222  Cave maps redesigned: start fully solid, carve horizontal corridors+chambers+shafts from rock (Worms Armageddon style).
0.5.4.220  Update screen smaller font; terrain sky clear on hills/canyon maps.
0.5.4.219  Garcia damage 45/hit; A release required before weapon charge.
0.5.4.197  Fix opponent name showing as your own in live matches; Propeller Hat blade scaled correctly.
0.5.4.196  Black Hole renders as swirling void; Plasma Torch fires in mid-air; CPU uses full weapon set; soldiers squirm in fire.
0.5.4.182  Propeller Hat blades now spin, with direction and speed following the current wind.
0.5.4.181  Cowboy Hat, Pirate Hat, Viking Helm, Beanie, Bandana, Angel Ring, Flamethrower, Rocket Launcher, and SMG cosmetics now all draw correctly on your soldier in-game (previously shop-icon only).
0.5.4.180  New cosmetics: Beanie, Bandana, and Angel Ring hats, plus an SMG gun style. Boot cosmetics (all colors) now use a redesigned sprite shape in-game.
0.5.4.179  Server-side: match logs now record fall damage and drowning deaths with proper labels, and disconnect/reconnect events include the player's name (no client-visible change).
0.5.4.177  Server-side: each live match now also logs to its own match-N.log file alongside the main server log (no client-visible change).
0.5.4.176  Server-side: game server now logs to a text file for easier debugging (no client-visible change).
0.5.4.175  Quitting a live ranked match now warns you that it counts as a loss with an ELO penalty before confirming.
0.5.4.174  Title screen now offers RECONNECT/ABANDON if you lose connection mid-match (not on a normal exit), letting you rejoin the same paused live match.
0.5.4.173  Live ranked matches now pause (up to 3 min) if your opponent's connection drops, letting them reconnect and resume the same match instead of forfeiting.
0.5.4.171  Redesigned hat/gun cosmetics with full pixel-art remakes, sprite-based boots and equip-screen icons, taller Wizard Hat, and 6 new cosmetics (Blue Party Hat, Cowboy Hat, Pirate Hat, Viking Helm, Revolver, Flamethrower, Rocket Launcher).
0.5.4.168  Update-available screen now shows only the 5 most recent changelog entries, drawn at 2x font size (was 24 lines at 1x).
0.5.4.167  Perf: fix background sky-band double-paint — the row-memcpy bg pass now only covers rows that are sky for every visible column (the screen's minimum sky_limit); columns with taller sky get their extra band filled once during the terrain copy instead of being painted twice, shrinking the "terrain+bg" pixel-write total on terrain with height variance (cliffs/canyons/floating islands).
0.5.4.166  Perf: rewrite the background sky-band copy (parallax layer) to use per-row slice copies instead of per-pixel reads/writes — this was the single largest pixel-write section ("terrain+bg") in the profiling overlay, and is now a handful of contiguous memcpys per frame instead of up to ~150k individual pixel ops.
0.5.4.165  Fix soldiers spawning in sealed/unescapable cave pockets on caverns maps: cave spawn placement now flood-fills (walk/fall/jump) from the candidate floor to verify it actually connects to an open-to-sky exit, not just that one exists somewhere nearby with walls in between.
0.5.4.164  Fix black borders on the 6 BG1.png-derived backgrounds (bg_0..3, bg_extra_city, bg_extra_pyramids): these slices had a ~3-9px near-black border baked in from the source contact sheet's grid lines, which scaled up to a visible black edge along the top/sides of the screen. The border is now cropped before scaling, so the art stretches to fill the screen.
0.5.4.163  Perf: merge background and terrain viewport copies for cave/chasm/overhang columns into a single pass — background now only paints the sky band above the terrain (sky_limit), and the terrain copy fills the gaps between solid spans with background pixels directly, so each pixel in the column is written once instead of twice. Preserves background parallax.
0.5.4.162  Add per-section pixel-write profiling overlay (TEST mode): a new on-screen breakdown shows how many pixels each render section (terrain/bg, water, soldiers, fire, projectiles, HUD, etc.) writes per frame, to help identify the next 30fps optimization target.
0.5.4.161  Restore background parallax without re-introducing pixelation: the background cache now stores the image at native 1:1 resolution (one cache column per source-image column), and the per-frame viewport copy re-samples it with a parallax-shifted (0.10) column offset, wrapping within the cached image width. Crater-driven cache updates removed (cache is no longer terrain-dependent).
0.5.4.160  Fix background pixelation: the 0.5.4.158 parallax fix stretched each source image column across ~10 world pixels (0.10 parallax factor applied directly to the world-x -> image-x mapping), making the art blocky. Background cache now maps world columns 1:1 to image columns (tiled); this layer scrolls 1:1 with the world like terrain, with no parallax.
0.5.4.159  Bazooka direct hits capped at 50 damage; bee stings reduced to 5 damage each; bazooka smoke trail now spawns from the rocket's tail instead of its nose; background images now bilinear-scaled (was nearest-neighbor, looked pixelated on upscale); perf: cave/chasm terrain columns now use precomputed solid spans in the viewport copy instead of a per-pixel is_solid check (~230k checks/frame removed).
0.5.4.158  Background cache now preserves the slow camera parallax (baked into the world-x -> image-x mapping, so it's still a one-time precompute) and the rotation pool includes all 6 BG1.png slices (15 total backgrounds, up from 13).
0.5.4.157  Major perf fix: the BG2 sky background was being re-rendered per-pixel from the source image every frame, painting down to the waterline (~640x400px) for every column with a cave/chasm/floating-island air gap. Now pre-rendered once per map into a world-space cache and stamped into the viewport with a handful of row memcpys per frame. Also: added background variants sliced from BG1.png to the rotation pool (was BG2.png's 9 only).
0.5.4.156  Perf: the every-frame framebuffer blit (180-degree screen rotation on Miyoo Mini Plus) reversed each row one byte at a time with per-byte indexing (640px x 4 bytes x 480 rows = ~1.2M indexed ops/frame); now copies whole 4-byte pixels via chunked slice copies. This runs unconditionally every frame regardless of scene contents, so it's the most likely remaining 25->30fps gap.
0.5.4.155  Perf: fire-patch flames now use unchecked pixel access for the common in-bounds case (was bounds-checking every pixel of every flame row, up to ~250 checks per burning fire/soldier each frame), closing more of the 25->30fps gap.
0.5.4.154  Fix soldiers unable to move on any uphill slope: the horizontal-move sweep checked each step against the soldier's current foot height with no step-up allowance, so even a 1px rise in terrain ahead halted movement entirely (projectiles, which don't use this check, were unaffected). The sweep now allows the same 0-8px step-up as the destination check.
0.5.4.153  Fix light-blue patches appearing on fresh maps (not just craters): spawn-mound headroom carving could punch a gap into solid ground without invalidating the sky_limit/solid_to_water cache, causing the viewport copy to block-copy stale cached pixels there. Perf: soldier skeleton line drawing now bounds-checks once per line instead of per pixel.
0.5.4.152  Fix soldiers still getting stuck on invisible terrain: is_on_ground/jump_unstick_lift now check the full 3-column body footprint (was center-only), matching the movement collision check. Fix camera shaking left-right during multi-explosion turns (was flip-flopping between explosions/airborne soldiers; now follows whichever is closest to current camera position). Perf: water surface now uses unchecked pixel access.
0.5.4.151  Fix stale light-blue patches over explosion craters (crater carve now recomputes sky_limit/solid_to_water so the viewport copy doesn't block-copy the old cached pixels over the hole); perf: viewport copy and sun-glow now use unchecked pixel access in their hot per-pixel loops, closing the rest of the 25->30fps gap.
0.5.4.150  Fix soldiers getting stuck on invisible terrain edges (surface-snap now checks the full 3-column body width, matching movement collision); fix 25fps regression from v149's ghosting fix (background air-gap paint now writes pixels directly instead of per-pixel bounds-checked set_pixel); dp.sh now also publishes the nginx build/zip and version.txt on every deploy.
0.5.4.149  Fix ghosting on maps with chasms/caves/craters: background now repaints air gaps below the surface (the v148 sky-band clip left them stale, showing the title screen / persistent wind particles & explosions / blacked-out terrain). Sky-band clip kept only for fully-solid columns.
0.5.4.148  New backgrounds: 9 painted skies (from BG2.png), one picked per map by seed for variety across all archetypes; backgrounds now only drawn in the sky band (perf); faster fill_rect/fill_circle (clamp-once + contiguous row writes) for crates/barrels/mines/fire/black holes
0.5.4.147  Tighten soldier/terrain collision (full body width+height checks while walking/airborne, fixes clipping through walls/ceilings); spawns now require the same full-body clearance so soldiers never start wedged in terrain; perf: block-copy fully-solid terrain columns in viewport copy
0.5.4.146  Fix: last-resort spawn mounds raised on previously-empty columns were never rendered (stale spawn_y/sky_limit) - soldier appeared to float above invisible terrain
0.5.4.145  Perf: water-surface fill no longer rewrites the ~110 already-correct body rows per column (was 640x122 set_pixel/frame, now ~640x10)
0.5.4.144  Perf: skip per-pixel sky checks above the terrain surface in viewport copy (iterate only from sky_limit down, instead of scanning the full sky band)
0.5.4.143  Floating-island maps (archetype 2) had ~57% empty columns - increase land density (lower density threshold) so terrain coverage is more reasonable
0.5.4.142  Fix invisible terrain on seeds with cliff overhangs/floating islands above y=80 (unclamped sky_limit field for renderer)
0.5.4.141  Restrict water-surface redraw to the visible viewport (was rendering all 1920 world columns every frame, 3x oversized)
0.5.4.140  Major perf fix: clip static bg image draw to on-screen columns (was rendering ~2M px/frame across hidden tiles)
0.5.4.139  FPS counter moved above timer; removed cloud/ridge landform layers (perf+visual)
0.5.4.138  Add FPS counter (bottom-right); optimize bg sky-fill/cloud/copy loops via spawn_y bound (skip per-pixel is_solid)
0.5.4.137  Remove landform layer (was hiding terrain + heavy perf cost); other bg perf/visibility fixes
0.5.4.136  Fix bg layering/perf: terrain visible again, debris spread across screen, faster static bg
0.5.4.133  Worms-style backgrounds: biome skies, sun, parallax hills, wind debris
0.5.4.132  Hand of Jerry sprite now fully white including the bolt
0.5.4.120  live parity: death blasts, graves, blood, crate types
0.5.4.94  update notes now served live
0.5.4.92  backflip animates every time
0.5.4.91  fix garbled terrain on chasm maps
0.5.4.90  full map seed shown in test mode
0.5.4.89  fix garbled textures (new tiles)
0.5.4.88  plasma torch, crate + texture fixes
0.5.4.82  plasma torch weapon
0.5.4.81  spread spawn mounds, no flat slab
0.5.4.80  charge meter from gun muzzle
0.5.4.78  even spawn spread, instant win on wipe
0.5.4.77  wider spawn spacing, seed shown in test
0.5.4.76  bigger maps (3 screens), smaller black hole
0.5.4.75  chasms central, fair team home landforms
0.5.4.74  team spawns cluster on real landforms
0.5.4.73  varied terrain: ledges, pits, water chasms
0.5.4.72  stronger hills, more headroom, labels up
0.5.4.71  rolling terrain, headroom, cave spawns
0.5.4.67  removed pillar maps
0.5.4.66  real caves, overhangs, sky islands
0.5.4.65  bigger maps, more hazards, MP sfx
0.5.4.64  no jump-clip through ground
v0.5.4.135: static background images (sunset/aurora/jungle/canyon by biome) as new lowest render layer
v0.5.4.237: live-mode parity infrastructure — emit_fx FX channel, unified net structs, parity test + compile-time field guard
